
version: 2


jobs:
  build:
    machine:
      node:
        version: 9.3.0
    steps:
      - checkout
      - run:
          name: npm i & npm test
          command: |
            sudo npm i
            sudo npm test