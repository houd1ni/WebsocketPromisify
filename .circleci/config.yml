
version: 2


jobs:
  build:
    machine:
      node:
        version: 9.3.0
    steps:
      - checkout
      - run:
          name: npm i & npm test
          command: |
            npm i
            npm test